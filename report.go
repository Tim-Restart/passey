package main

import (
	"fmt"
)

// Report function to provide a format for printing the report
// Initally the output will be to HTML (possibly only option)
//

func (dt *details) reportHTML() error {
	fmt.Println("Report function working")
	return nil
}
